name: Publish

on:
  workflow_dispatch:
  push:
    tags:
      - "v*.*.*"

jobs:
  publish:
    permissions:
      contents: write
    uses: mareuter/shared-workflows/.github/workflows/publish-tauri.yaml@main
